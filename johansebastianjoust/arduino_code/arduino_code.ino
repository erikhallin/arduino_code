bool alive=true;
const int buzzer = 6;
const int ledr=9;
const int ledg=10;
const int ledb=11;

void setup() 
{
  //pinMode(buzzer, OUTPUT); 
  pinMode(ledr, OUTPUT); 
  pinMode(ledg, OUTPUT); 
  pinMode(ledb, OUTPUT); 
  
  //Init RGB, set color

  //Summer

  //Acc

  //temp output

}

void loop() 
{
  /*tone(buzzer, 1000);   // set the LED on
  delay(1000);              // wait for a second
  noTone(buzzer);    // set the LED off
  delay(1000);              // wait for a second*/

  /*analogWrite(ledr, 150);
  analogWrite(ledg, 0);
  analogWrite(ledb, 250);
  delay(1000);
  analogWrite(ledr, 250);
  analogWrite(ledg, 100);
  analogWrite(ledb, 100);
  delay(1000);*/
  
  /*if(alive)
  {
    //check Acc input
  
    //if not alive
    {
      alive=false;
      
      //set LED color
      {

      }
      

      //Summer beep
      {

      }
    }
  }*/

}
